# Biến dùng cho docker-compose (thay thế ${...} trong docker-compose.yml)
# Copy thành .env:  cp .env.example .env
DB_DATABASE=anh_ngu
DB_USERNAME=anhngu
DB_PASSWORD=secret
DB_ROOT_PASSWORD=root

NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
